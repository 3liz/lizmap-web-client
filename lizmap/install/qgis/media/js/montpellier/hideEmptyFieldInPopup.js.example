lizMap.events.on({

    'lizmappopupdisplayed': function(e){
        $('table.lizmapPopupTable td').each(function(){
            if( $(this).html() == '' || $(this).html() == 'f' )
                $(this).parent('tr').hide();
        });
    }

});
